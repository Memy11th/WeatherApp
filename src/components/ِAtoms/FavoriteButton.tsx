import { WeatherData } from '@/interfaces/WeatherResponse'
import React from 'react'

const FavoriteButton = ({data}:{data:WeatherData}) => {
    console.log(data)
    return <>
            
    </>
}

export default FavoriteButton
